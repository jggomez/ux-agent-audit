[project]
name = "ux-ui-audit-agent"
version = "0.1.0"
description = "An automated UX/UI Audit Agent application using the google-antigravity SDK."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-antigravity>=0.1.3",
    "pillow>=10.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
